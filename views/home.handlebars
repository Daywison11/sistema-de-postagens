<div class="nav">
    <h1>Lista de Posts</h1>
    
    <a href="/cadastros">Cadastrar novo post</a>
</div>

<div class="fundo">
    {{#each posts}}
    
        <div class="pt">
    
            <small>{{{dataValues.createdAt}}}</small>
            <h1>{{dataValues.titulo}}</h1>
            <p>{{dataValues.conteudo}}</p>
            <a href="/deletar/{{dataValues.id}}"><button>deletar</button></a>
    
        </div>
    {{/each}}
</div>

<style>
    *{
        margin: 0;
    }
    .fundo{
        display: flex;
        overflow: hidden;
        flex-wrap: wrap;
        justify-content: space-evenly;
        width: 100%;
    }
    .nav{
        display: flex;
        justify-content: space-between;
        align-items: center;
        background-color: lightslategray;
        padding: 30px;
    }
    .nav >a {
        text-decoration: none;
        color: white;
    }
    .pt{
        margin:10px;
        text-align: center;
        overflow: hidden;
        max-width: 300px;
        min-width: 300px;
        min-height: 100px;
        border: 1px solid lightblue;
        word-break: break-all;
    }
    h1{
        font-size: 25px;
    }

</style>